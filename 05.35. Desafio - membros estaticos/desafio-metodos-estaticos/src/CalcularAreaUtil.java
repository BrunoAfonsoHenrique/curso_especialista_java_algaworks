public class CalcularAreaUtil {

    static double calcularAreaQuadrado(double area) {
        return Math.pow(area, 2);
    }

    static double calcularAreaCirculo(double raio) {
        return (Math.pow(raio, 2)) * Math.PI;
    }

}
